var log         		= require( 'libs/log' )( module );
var app    				= require( 'app' );
var locationCore    	= require( 'core/location' );
var frontCore			= require( 'core/front' );


var init = function( uid, questName ) {

}

module.exports.init = init;